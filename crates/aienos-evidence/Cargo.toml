[package]
name = "aienos-evidence"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Host-side Config A capture and evidence verification for the AIENOS reference freeze"

[dependencies]
aienos-kernel = { workspace = true }
serde_json = { version = "1.0", features = ["std"] }

[[bin]]
name = "aienos-evidence"
path = "src/main.rs"
