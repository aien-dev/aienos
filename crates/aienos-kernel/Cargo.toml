[package]
name = "aienos-kernel"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "AIENOS bare-metal no_std kernel substrate for aarch64"

[features]
default = ["std"]
std = []

[dependencies]
